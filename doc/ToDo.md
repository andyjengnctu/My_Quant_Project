## Going

1. 之後如果我只丟檔案，沒有文字指令，就代表檢查ok就多走幾步
2. 將目前的狀態回報方式也寫進文件，方便之後我後我跟你的溝通。
3. 將優化測試程式本身整體測試速度，也納入checklist

## B. 驗證與本地回歸

1. 若使用者只傳檔案、ZIP上來，沒有文字指示，預設就是代表進行檢查。
2. 每輪檢查必須先產出「專案設定條文覆蓋表」，逐條列出檢查條文編號與內容、狀態(Pass/Fail/Skip/Fail->Pass)、狀態說明、修正方法；在覆蓋表完成前，不得直接進入局部修補或交付 patch。
3. 每輪檢查必必須產出「未定義風檢蓋表」，列出所有具有風險，但未包含在專案設定中的項目，處理同 B.2。 
4. 檢查必須先依問題類型、被破壞的 invariant 與影響面做完整盤點，一次找出所有相關問題與變體；未完成前不得交付 patch 或宣稱完成。後續若再發現同類變體，視為前輪未完成；不得以多輪局部修補當成預設流程。
5. `apps/test_suite.py` 為本專案預設的動態回歸正式入口，僅限定在本地端執行，不得在 GPT 端執行。
6. `apps/test_suite.py` 限定使用 `data/tw_stock_data_vip_reduced` 進行測試。
7. assistant 不得重複執行或重複驗證 `apps/test_suite.py` 已涵蓋之測試項目；
8. assistant 僅可對 `apps/test_suite.py` 未涵蓋、且確有必要、且無法由靜態檢查完成之項目進行補充驗證；且不得因此執行或重建 `apps/test_suite.py` 本體流程。
9. assistant 須協助使用者完善 `apps/test_suite.py` 在本地端的動態測試；若發現其未覆蓋本專案設定，必須明確指出缺口、風險與修正方法，以節省在 GPT 上的測試時間。

- 先提升訓練分數
    - 將訓練參數集中，包含是否停利
    - 將限制式集中
    - final score 分母包含總報酬率選項
- 訓練/測試樣本分離
- overfitting量化驗證工具

## To do
- ajd/raw整合交易策略 
- 加入大盤過濾
- 加入低點買入 + 盤整期策略
- 快速檢視不同獎勵函式結果 (排行）
- 改為線上自動交易模擬 (依當天資產決定持有股數、沒買到下一檔(現在就是))
- online虛擬交易 (小時交易)
- TV-based UI
- LLM-based learning
- 自動壓力測試角本，驗證長時間運作穩定性

## Pending (useless)
- 訓練歷史績效參數
- 考慮資投入資金效率、持有天數 
- 更新EV算法與對應訓練方法

## Done
- 實際交易模擬
- 並訓練增/減不同條件 (0/1)
- Git -> 匯入程式碼操作
- 俢正downloader (刪除不符合的)
- 是否打敗大盤
- 檔案架構整理
- 直接訓練最終指標與買進策略 (end-to-end approach)
- 過濾一字鎖死漲停還能買入
- 顯示買到/沒買到的次數
- 跌停鎖死當天是賣不掉的、後面可以繼續賣、顯示未成功賣出筆數
- 買入訊號依"可投入最多"資產，也就是考慮初始停損與股價
- 將歷史績效過濾條件、風險比也納入end-to-end架構訓練
- 解決當天無法買入真實性問題 
- 使買入限價/初始停損/停利/移動停損符合開盤前操作的限制，並完美符合R的交互關係
- 更新交易次數的penality算法 (不使用固定值)
- 顯示年化報酬率
- 加入winrate * count買入排序